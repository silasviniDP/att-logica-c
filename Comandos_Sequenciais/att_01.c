#include <stdio.h>

int main () { 
    int x, y;

    printf("Digite dois numeros e descubra o produto entre eles: ");
    scanf("%d %d", &x, &y);
    printf("O valor da multiplicação é: %d\n", x * y);

    return 0;
}
